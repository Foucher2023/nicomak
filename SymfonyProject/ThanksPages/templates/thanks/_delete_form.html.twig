<div class="button-section">
<form method="post" action="{{ path('app_thanks_delete', {'id': thank.id}) }}" onsubmit="return confirm(' êtes vous sûr de vouloir supprimer ce Merci ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ thank.id) }}">
        <a>Supprimer ce "Merci" </a>
</form>
</div>
